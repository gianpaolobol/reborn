# Role Dashboards Prototype Notes

The prototype API client now includes:

```js
REBORN_API.dashboard()
REBORN_API.roleDashboard('maker')
REBORN_API.roleDashboard('provider')
```

The current static prototype can use these methods to render role dashboards once the next UI iteration adds login state, role switching and authenticated create-case flows.

## Next UI work

- Login panel in the prototype shell.
- Persisted role indicator.
- Repair user dashboard route.
- Maker opportunity route.
- Provider fulfilment queue route.
- Enterprise cockpit route.
- Admin control room route.
