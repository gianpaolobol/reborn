# Public Entry

The MVP frontend should start simple:

- server-rendered PHP pages;
- HTML5;
- CSS3;
- vanilla JavaScript;
- no frontend framework;
- no Bootstrap.

Later, if the product requires it, selected screens can become richer client-side modules.
